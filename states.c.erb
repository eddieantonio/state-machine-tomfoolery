/**
 * states.c -- AUTOGENERATED FILE. Do not modify.
 */
#include <stdlib.h>

enum accept {
    INVALID, RNA, DNA, PROTEIN
};

struct state {
    enum accept accept;
    struct state* transition[26];
};

<% states.each_with_index do | state, idx | %>
static struct state state_<%= idx %>;
<% end %>

<% states.each_with_index do | state, idx | %>
static struct state state_<%= idx %> = {
    .accept = <%= state.accept %>,
    .transition = { <% state.unwrap.each do | char, target | %>
        <%= target ? "&state_#{target}" : '    NULL' %>, /* <%= char %> */ <% end %>
    }
};

<% end %>
